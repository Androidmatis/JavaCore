// Реализовать MyIF.
class MyIFImp implements MyIF {
    // Необходимо реализовать только метод getNumber(), определенный в MyIF.
    // Для метода getString() разрешено принять стандартную реализацию.
    public int getNumber() {
        return 100;
    }
}
